---
import Navbar from './Navbar.astro'
import sectionOptions from '../../lib/section-options'

type Props = {
    title: string
    className?: string
}

const options = sectionOptions.firstSection

const { title, className } = Astro.props

---

<Navbar options={options} title={title} className={className} navClassName='w-full mb-2' />